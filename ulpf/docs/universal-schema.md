# Universal Schema

## Purpose
The internal universal schema is a project-specific canonical model designed to standardize heterogeneous log fields into common top-level semantic areas such as:
- event.*
- source.*
- destination.*
- network.*
- user.*
- device.*
- severity

This ensures downstream components have a predictable and consistent data structure to work with.
